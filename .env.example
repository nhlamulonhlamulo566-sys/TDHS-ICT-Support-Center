# Firebase Admin SDK Credentials (base64 encoded JSON)
# To generate this value from your Firebase service account JSON:
# node -e "const fs = require('fs'); const content = fs.readFileSync('your-credentials.json', 'utf-8'); const encoded = Buffer.from(content).toString('base64'); console.log('FIREBASE_ADMIN_SDK_JSON=' + encoded);"
FIREBASE_ADMIN_SDK_JSON=

# For local development, you can alternatively use:
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/credentials.json

# Google Gemini API Key
GEMINI_API_KEY=
